# site-forge

A registry and CLI for shipping independent websites in minutes. Pick a preset, and site-forge
copies a Next.js starter plus exactly the modules and UI sections the site needs into a fresh
repository that you own completely.

> Work in progress. See `CLAUDE.md` for conventions and `docs/` for guides.
